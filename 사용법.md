# 사용법 (처음 쓰는 사람용)

이 킷으로 OVERDARE 게임에 1인칭 팔(뷰모델)을 붙이는 순서다. 위에서부터 차례대로 하면 된다.

---

## 0. 받기

- **ZIP** : 레포 첫 화면 초록색 `Code` 버튼 → `Download ZIP` → 압축 풀기
- **git** : `git clone https://github.com/ShootTheMoon/viewmodelkit_for_OVDR.git`

필요한 것 : OVERDARE Studio, Blender (4.x 에서 확인), 클립을 직접 뽑을 거면 Blender 만 있으면 된다 (파이썬 따로 설치 필요 없음).

---

## 1. 팔 모델 넣기

1. OVERDARE Studio 에서 `blender/VMKit_Arms.fbx` 를 임포트한다.
   무기가 있으면 블렌더에서 팔과 한 파일로 합쳐서 내보내는 게 편하다 (`tools/export_maxico_arms.py` 참고).
2. 임포트된 모델을 **Workspace** 에 둔다. 모델 이름을 기억해 둔다 (예 : `MyChar_Arms`).
3. 안의 파츠 이름이 `R_UpperArm`, `R_LowerArm`, `R_Hand`, `L_UpperArm`, `L_LowerArm`, `L_Hand` 인지 확인한다.
   **이름을 바꾸면 애니메이션이 안 붙는다.**

> 폴리곤 한도 : OVERDARE 는 모델 하나에 삼각형 30,000 개까지. 팔은 454 개라 여유 있고, 무기가 무거우면 블렌더에서 Decimate 로 줄인다.

---

## 2. 스크립트 넣기

`lua/` 안의 파일을 아래 위치에 **같은 이름**으로 만든다. 내용은 파일을 열어 통째로 복사해서 붙여넣기.

| 파일 | 종류 | 위치 |
|---|---|---|
| `ViewmodelController.lua` | LocalScript | StarterPlayer / StarterPlayerScripts |
| `FirstPersonLock.lua` | LocalScript | StarterPlayer / StarterPlayerScripts |
| `ViewmodelConfig.lua` | ModuleScript | ReplicatedStorage |
| `ViewmodelAnimData.lua` | ModuleScript | ReplicatedStorage |
| `clips/ViewmodelAnim*.lua` | ModuleScript | ReplicatedStorage (파일 이름 그대로, `.lua` 뺀 이름) |

클립 모듈은 컨트롤러가 `ReplicatedStorage` 에서 `"ViewmodelAnim" .. 이름` 으로 찾는다.
예 : 설정에 `Intro = "Maxico1Intro"` 라고 쓰면 `ViewmodelAnimMaxico1Intro` 모듈을 찾는다.

> ONLY ONE TAP 전용 코드(마쿠아후이틀 금색 판, 찌르기, 장풍, 서버 판정 등)도 컨트롤러 안에 같이 들어 있다.
> 그 부분은 서버 스크립트(CombatServer 등)가 없으면 그냥 동작하지 않을 뿐이고, 팔 표시·애니메이션은 따로 돈다.

---

## 3. 내 캐릭터 등록하기 (`ViewmodelConfig`)

`CHARACTERS` 표에 한 칸 추가한다. 기존 `maxico` 칸을 복사해서 고치면 제일 쉽다.

```lua
mychar = {
    SOURCE = "MyChar_Arms",                 -- 1번에서 Workspace 에 둔 모델 이름
    PIVOT  = { 927.2576, 72.7878, 1040.1578 }, -- 4번에서 잰 값
    OFFSET = { -10, -80, -75 },             -- 화면 위치 (cm) X 오른쪽+ / Y 위+ / Z 앞-
    SHRINK = 0.34,                          -- 벽·바닥에 붙었을 때 최대 얼마나 작아질지
    YAW    = 0,                             -- 팔이 뒤를 보고 있으면 180
    ANIM   = true,
    CLIPS  = {
        Intro    = "MyCharIntro",
        Attack1  = "MyCharAttack1",
        Attack2  = "MyCharAttack2",         -- Attack1..n 이 있는 만큼 연타가 이어진다
        BlockIn  = "MyCharBlockIn",
        BlockOut = "MyCharBlockOut",
        RunStart = "MyCharRunStart",
        RunLoop  = "MyCharRunLoop",
        RunStop  = "MyCharRunStop",
    },
},
```

- `CLIPS` 에 **적힌 것만** 재생된다. 없는 동작은 그냥 대기자세로 있는다.
- 어떤 칸을 쓸지는 `_G.MyPick` 값으로 정해진다 (ONLY ONE TAP 에서는 로비가 넣어준다). 값이 없으면 `"japan"` 칸을 쓴다.
  로비 없이 시험하려면 클라이언트 스크립트에서 `_G.MyPick = "mychar"` 를 먼저 넣어주면 된다.

---

## 4. PIVOT 재기 (제일 많이 틀리는 곳)

PIVOT 은 **레벨 안의 절대 좌표**다. 모델을 다시 임포트하거나 옮기면 그 즉시 틀린 값이 된다.

- 틀리면 증상 : 팔이 화면 한쪽으로 쳐박히거나 아예 안 보인다. OFFSET 을 아무리 바꿔도 안 고쳐진다.
- 재는 법 : 스튜디오에서 모델 안 `R_UpperArm` 과 `L_UpperArm` 의 Position 을 읽어 **둘의 중점**을 넣는다.
  블렌더에서 클립을 뽑을 때 쓴 기준점 `O` 가 따로 있으면, 그 점을 레벨 좌표로 옮긴 값을 넣는다
  (블렌더 → 레벨 : `레벨 = 100 × (-x, z, y) + 모델 위치`).

---

## 5. 화면 맞추기

Play 를 누르고 보면서 `ViewmodelConfig` 숫자만 고친다. 스크립트는 안 건드려도 된다.

| 증상 | 고칠 값 |
|---|---|
| 팔이 너무 오른쪽/왼쪽 | `OFFSET` 첫 번째 (X) |
| 너무 위/아래 | `OFFSET` 두 번째 (Y) |
| 너무 가깝다/멀다 | `OFFSET` 세 번째 (Z, 앞이 -) |
| 팔 뒤통수가 보인다 | `YAW = 180` |
| 벽·바닥에 팔이 파고든다 | `SHRINK` 를 더 작게 (0.3 근처) |

> 뷰모델은 월드에 놓인 진짜 물체라 벽을 뚫을 수 있다. 컨트롤러가 매 프레임 앞쪽 공간을 재서
> 공간이 좁으면 카메라 중심으로 작게 줄인다. 원근 때문에 화면에서는 크기가 거의 그대로 보인다.

---

## 6. 애니메이션 클립 만들기 (블렌더 → OVERDARE)

1. 블렌더에서 팔(+무기) 애니메이션을 만든다. **30fps**, 파츠 이름은 1번과 같게.
   - 기준 프레임(보통 첫 프레임)은 **인게임 대기자세와 똑같아야** 한다.
2. `tools/make_maxico_block.py` 를 복사해서 윗부분만 고친다.
   - `BLEND` : 내 .blend 경로
   - `OUTD` : 결과 저장 폴더
   - `REST_F` : 기준 프레임 번호
   - `O` : 기준점 (4번의 PIVOT 과 같은 점, 블렌더 좌표)
   - `SEGS` : `("클립이름", 시작프레임, 끝프레임)` 목록
3. 실행 : `blender --background --python make_mychar_xxx.py`
   - 실행하면 "기준 프레임 팔 ↔ 게임 모델 : 최대 0.00 cm" 같은 줄이 찍힌다. **0 에 가까워야 정상**이다.
     크면 기준 프레임 자세가 인게임 모델과 다르다는 뜻이다.
4. 나온 `.lua` 를 `ViewmodelAnim클립이름` ModuleScript 로 ReplicatedStorage 에 넣고, 3번 `CLIPS` 에 이름을 적는다.

클립 안의 숫자가 뭔지는 README 의 "클립 형식" 참고.

---

## 7. 킷의 팔 모델을 다시 만들고 싶을 때

`tools/build_vmkit.py` 가 `Mixamo_For_OVDR_Arms_armbones.fbx` 에서 뼈를 버리고 팔 6조각으로 자르는 스크립트다.
맨 위 FBX 경로와 맨 아래 저장 경로(`K`)만 바꿔서 `blender --background --python build_vmkit.py` 로 돌린다.

---

## 자주 막히는 것

- **팔이 안 보인다** → `SOURCE` 이름이 Workspace 모델 이름과 같은지, PIVOT 이 지금 모델 위치 기준인지.
- **애니메이션이 안 나온다** → 클립 모듈 이름이 `ViewmodelAnim` + CLIPS 에 적은 이름인지, ReplicatedStorage 에 있는지.
- **팔 조각이 따로 논다** → 파츠 이름이 바뀌었거나, 클립을 뽑은 기준 프레임이 대기자세와 다르다.
- **Mixamo FBX 애니메이션을 그대로 임포트** → OVERDARE 는 이걸 새 커스텀 스켈레톤으로 만들어버려서 기본 아바타에 안 붙는다. 이 킷처럼 파츠 CFrame 클립으로 뽑는 걸 추천.
- **OVERDARE 는 Roblox 가 아니다** → 비슷한 API 라도 없거나 다르게 동작하는 게 있다 (예 : ViewportFrame, Highlight 없음). 확인하고 쓸 것.

---

## 이 레포에 수정 올리기 (팀원용)

```bash
git pull                      # 최신 받기
# 파일 수정
git add -A
git commit -m "무엇을 바꿨는지"
git push
```
